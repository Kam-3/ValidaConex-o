# Validação de Conexão com API Gemini

Este projeto é um script simples em Python para validar a comunicação com a API do Google Generative AI.

## Como rodar
1. Instale as dependências: `pip install -r requirements.txt`
2. Insira sua chave de API no script.
3. Execute: `python valida_conexao.py`